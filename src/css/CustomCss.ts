import { StyleSheet } from 'react-native';

export const custom_styles = StyleSheet.create({
  container: { flex: 1, padding: 20, gap: 24 },
  centered_container: {
    flex: 1,
    alignItems: 'center',
    padding: 20,
    gap: 24,
  },
});
